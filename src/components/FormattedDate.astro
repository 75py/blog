---
const date = Astro.props.date instanceof Date ? Astro.props.date : new Date(Astro.props.date)
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('ja-JP', {
			year: 'numeric',
			month: '2-digit',
			day: '2-digit',
            weekday: 'short',
		})
	}
</time>
